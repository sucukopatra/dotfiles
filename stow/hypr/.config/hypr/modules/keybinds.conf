# PROGRAMS
$terminal = kitty
$browser = zen-browser
$fileManager = nautilus
$menu = rofi

$mainMod = SUPER
$altMod = ALT

bind = $mainMod, RETURN, exec, $terminal
bind = $mainMod, Q, killactive,

bind = $mainMod, E, exec, $fileManager
bind = $mainMod, S, exec, $browser
bind = $altMod, SPACE, exec, $menu -show drun
bind = $mainMod, G, exec, ~/.config/hypr/scripts/volumecontrol_toggle.sh

bind = $mainMod, V, togglefloating,
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, U, layoutmsg, togglesplit # dwindle

bind = $mainMod, M, exec, command -v hyprshutdown >/dev/null 2>&1 && hyprshutdown || hyprctl dispatch exit

bind = $mainMod, B, exec, ~/.config/hypr/scripts/btconnect_toggle.sh
bind = $mainMod SHIFT, B, exec, ~/.config/hypr/scripts/change_gpu.sh

bind = , PRINT, exec, pidof slurp || if area=$(slurp); then grim -g "$area" - | tee >(wl-copy) > $(xdg-user-dir PICTURES)/screenshots/$(date +'%s_grim.png') && dunstify "Screenshot of the region taken" -t 1000; fi
bind = $altMod, PRINT, exec, grim - | tee >(wl-copy) > $(xdg-user-dir PICTURES)/screenshots/$(date +'%s_grim.png') && dunstify "Screenshot of the screen taken" -t 1000

# Fullscreen
bind = $mainMod, F, fullscreenstate, 1, 1
bind = $mainMod SHIFT, F, fullscreenstate, 2, 2

# Cycling
bind = $altMod, TAB, cyclenext
bind = $altMod SHIFT, TAB, cyclenext, prev
bind = $mainMod, TAB, workspace, +1
bind = $mainMod SHIFT, TAB, workspace, -1

# Move focus
bind = $mainMod, H, movefocus, l
bind = $mainMod, L, movefocus, r
bind = $mainMod, K, movefocus, u
bind = $mainMod, J, movefocus, d

# Move windows
bind = $altMod, H, movewindow, l
bind = $altMod, L, movewindow, r
bind = $altMod, K, movewindow, u
bind = $altMod, J, movewindow, d
bindm = $mainMod, mouse:272, movewindow

# Resize windows
binde = $mainMod SHIFT, h, resizeactive,-50 0
binde = $mainMod SHIFT, l, resizeactive, 50 0
binde = $mainMod SHIFT, k, resizeactive, 0 -50
binde = $mainMod SHIFT, j, resizeactive, 0 50
bindm = $mainMod, mouse:273, resizewindow

# Switch workspaces
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move window to a workspace
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Special workspace (scratchpad)
bind = $mainMod SHIFT, S, togglespecialworkspace, :magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1


# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -d intel_backlight -e4 -n2 set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -d intel_backlight -e4 -n2 set 5%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

